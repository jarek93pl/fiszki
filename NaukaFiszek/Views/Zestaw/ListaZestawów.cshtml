@{
    Layout = null;
}
@model IEnumerable<DTO.SetFiszka>

<p>
    <a href=@Url.Action("DodajZestaw","Zestaw") id="addSet">Dodaj zestaw</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr class="SetElement" id=@item.Id>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td class="">
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Usuń", "Delete", null, new { id = item.Id, @class = "Remuver" })
            </td>
        </tr>
    }

</table>

<script src="~/Scripts/Zestaw/ListaZestawów.js"></script>
